package com.kontinum.routing.core

import io.ktor.server.application.*
import io.ktor.server.routing.*

fun Application.stocksRouting() {
    routing {
        post("/stock") {  }

        get("/stock") {  }

        put("/stock") {  }

        patch("/stock") {  }

        delete("/stock") {  }
    }
}